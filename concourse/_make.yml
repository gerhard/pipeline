platform: linux

image_resource:
  type: docker-image
  source:
    repository: pivotalrabbitmq/cf-cli

inputs:
- name: pipeline

run:
  path: ./pipeline/concourse/make.sh
  dir: pipeline
  args:
    - public
