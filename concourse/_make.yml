platform: linux

image_resource:
  type: docker-image
  source:
    repository: pivotalrabbitmq/cf-cli

inputs:
- name: slides

run:
  path: ./slides/concourse/make.sh
  dir: slides
  args:
    - public
